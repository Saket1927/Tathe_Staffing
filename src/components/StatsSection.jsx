// The 3-stat panel is now seamlessly integrated into the Hero component
// directly matching the visual reference image. This component returns null
// to avoid duplicate statistics rendering while preserving imports.
export default function StatsSection() {
  return null;
}
